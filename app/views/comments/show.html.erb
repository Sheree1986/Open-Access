<div class="container">
  <div class="row">
    <div class="offset-md-2 col-md-8">
      <div class="card card-block">
        <center><h4 class="card-title">Comments for <%= @blog.user.username.capitalize %></h4></center>
         <center><p class="card-text"><%= @comment.content %></p>
         
      
        <% if @blog.user == current_user %>
          | <%= link_to 'Back', blogs_path, class: "btn btn-primary"%> |
          <%= link_to 'Edit', edit_blog_path(@blog), class: "card-link", class:"btn btn-success" %> |
          <%= link_to 'Delete', @blog, method: :delete, class: "card-link", class: "btn btn-danger" %> |

          <% end %>
            </div>
             </div>
          </div>
        </div>
        
 <center>
          <div id="comments">
          
            <% if @blog.user %>
            <h1>Comments</h1>
            <%= render @blog.comments %></center>
          
         <center><h2>Add a Comment</h2></center>
          <%= render partial: "comments/form", locals: {comment: @blog.comments.new} %>
        </div> </center>

        
        
<center>
        |<%= link_to 'Back', blogs_path, class: "btn btn-primary"%> |</center>

        <% end %>
           
</div>

</center>

